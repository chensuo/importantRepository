1、上午，解决上次工作的遗留问题，优化mobile模板搜索结果页面的显示，添加在搜索结果页着重显示所搜的关键字效果。
2、13.00-15.00，在搜索结果页面添加返回按钮。
3、15.00-17.30，解决搜索结果页面返回按钮的返回路径问题，分为两部分，若是从所搜索的结果列表中进入再返回，则该
搜索结果页的返回按钮是返回于主页面，第二种是并未进入搜索结果页，则该搜索结果页的返回按钮是返回上一页面并刷新。
主要问题在于${base}并不能适用于js，解决方案直接更改a标签的href。
4、17.30-18.00，总结工作内容，备份代码。